target/
node_modules/
playwright-report/
test-results/
**/*.rs.bk
.DS_Store
.idea/
.vscode/
.claude/command-history.log
*.swp
Propresenter library/
Libraries/
presenter_*.db
presenter_*.db-shm
presenter_*.db-wal

logs/
var/
gateway/node_modules/

ops/companion/generated/

test
test2

.playwright-mcp/
bible-ui-*.png
.superpowers/

# Claude projects metadata + memory (per-machine, not source code)
.claude/projects/

# Vendored NDI SDK (license-restricted; install separately via runbook)
NDI SDK for Linux/

# Ad-hoc screenshots written to repo root during dev sessions
/*.png
/*.jpeg
/*.jpg

# cargo-mutants output + the diff the in-diff PR gate writes (CI + local runs).
# mutants.out.old/ is the previous run's output that cargo-mutants rotates aside.
mutants.out/
mutants.out.old/
/pr.diff

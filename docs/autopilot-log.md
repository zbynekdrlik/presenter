# Autopilot Log

Terse per-issue record of autonomous cycles (issue #, commits, tests, decisions).

---

## 2026-06-22 — #346 Decompose state/mod.rs AppState facade → subsystem managers (PR #456, v0.4.153)

- **Validated still real:** `crates/presenter-server/src/state/mod.rs` = 1115 lines, `AppState` = 24 fields. NOT a 1000-line-cap fix — the CI fn-size gate miscounts this file (stops at the first `#[cfg(test)]` ~line 33; that undercount is open #407). So done as voluntary readability decomposition, not gate compliance.
- **Version:** 0.4.152 → **0.4.153** (first commit `15ba9ab3`), after FF dev→main `0740b8d2`.
- **Refactor (commit `4676e844`, `Closes #346`):** behavior-preserving — extracted 3 cohesive manager structs into new `state/` modules, shrinking AppState **24 → 18 fields**. `CacheManager` (`cache_manager.rs`) owns the 3 `Arc<RwLock>` caches (`presentation`/`group_color`/`ableset`); `CompanionManager` (`companion_manager.rs`) owns `token`/`enabled`(AtomicBool)/`port`(AtomicU16)/`server`; `BibleManager` (`bible_manager.rs`) owns `broadcast`/`slide_output` `Arc<RwLock>` + the `#[cfg(test)] ingestion_override`. Pure field relocation: every Arc/RwLock/atomic keeps its exact type → single-lock-at-a-time policy + Ordering::SeqCst + heartbeat/background-task/NDI-restore wiring unchanged; `clear_bible_broadcast` still takes the 2 bible locks in separate scoped blocks. All access is encapsulated within the `state/` module tree (submodules ableset.rs/bible.rs/presentations.rs use `self.caches.*`/`self.bible.*`); public accessors (`active_bible_broadcast`, `companion_enabled`, etc.) unchanged → zero cross-crate call-site churn.
- **No new tests (no-behavior-change refactor):** existing `state/tests.rs` + `router/tests.rs` are the net (active/clear_bible_broadcast, set_test_bible_ingestion, `state.api_stage` direct read at tests.rs:665 — `api_stage` deliberately left inline so that test stays untouched). Marker commits `f64b7198`/`9b8769fd` carry `[no-test: ...]` per the pre-push hook contract (the multi-line bracket form is invisible to the single-line hook regex — must be one line).
- **Local gates:** `cargo fmt --all --check` clean; `cargo check -p presenter-server --all-targets` clean; `cargo clippy -p presenter-server --all-targets -D warnings` clean (0 warnings).
- **Shared branch:** `dev` concurrently owned by the #347 worker — its docs commit `d5439b23` (autopilot-log) rode onto this PR's diff (docs-only, no `Closes #347`); harmless.

## 2026-06-22 — #347 Migrate settings_script.js (1307-line JS blob) → Leptos WASM component

- **Validated still real:** `settings_script.js` = 1307 lines, included via `ui/scripts/mod.rs`, injected in `ui/settings.rs:883`; route `/ui/settings` was pure SSR. The orphan `pages/settings.rs` (185-line NDI-only `SettingsPage`, zero callers) was NOT the migration — folded its NDI piece into the full page.
- **Version:** 0.4.151 → **0.4.152** (first commit `b8ad2bb4`), after FF dev→main `aa6686ff`.
- **Implementation (commit `a48949c7`, PR #454 → merge `0740b8d2`):** built `pages/settings/` Leptos module (mod + companion/preferences/resolume/android/ableton/video_sources cards) driven by signals + `on:*` + the existing `api::settings`/`api::ndi` client fns; added the resolume/android/osc/feature client fns + camelCase DTOs to `api/settings.rs`; wired `/ui/settings` → WASM shell (router + lib.rs route); folded settings CSS into the WASM bundle (`styles/settings.css` + index.html). **Deleted −1809 lines net:** `settings_script.js`, SSR `ui/settings.rs` (918), orphan `ui/components/settings.rs` (608), `ui/scripts/mod.rs`, `ui/styles/settings.css`, `escape_script_tag`, `styles::SETTINGS`, `AppState::feature_flags()`. 5s status poll, toast, and the #272 line-limit pref preserved (now keyed `presenter:lineLimit` to match what the operator reads — fixed a latent cross-page key mismatch). Split into 7 submodules to stay under the 1000-line file cap (`#[component]` fns are fn-length-exempt).
- **Tests:** `tests/e2e/settings.spec.ts` updated to wait for `data-wasm-ready` + a migration safety-net test (all cards render, version label, toast on save, line-limit persists, **0 console errors**). `quality-check.sh` §3 UI-page gate dropped `settings` (migrated to WASM, like tablet); `router/tests.rs` → `settings_route_serves_wasm_shell` (accepts 503 when bundle unbuilt).
- **Gates:** full Pipeline green — Clippy/Format/TS/Quality/Version/Coverage/Build, all 16 mutation shards, all 3 Playwright E2E shards, NDI synthetic E2E, Deploy-Dev + Deploy. mergeable=clean.
- **Deploy verified (Playwright):** prod SNV `http://10.77.9.205/ui/settings` — WASM mounts, version label `v0.4.152` (matches deployed prod binary), all 6 cards render, real settings read back (3 resolume hosts, 4 android displays, ableset host `fohabl.lan`, OSC 39051, companion 18175), 0 console errors. Dev also verified `v0.4.152 (dev)`.
- **Post-merge review findings → #455** (could not land in the already-merged #454; `dev` was concurrently owned by the #346 worker): (1) `parse_port` overflow silently substitutes fallback for >65535 ports (should reject); (2) video_sources activate/deactivate/delete discard the Result → false success toast. Both small, fully described + patch preserved on #455.

## 2026-06-16 — #374 CI: function-length gate self-test (regression guard for abs/rel no-op)

- **Scope (rescoped by ticket-validator):** only the remaining-open item — add a self-test so the function-length gate's abs/rel no-op bug can't silently return. Gate fix (`5f5fe9f`) + the 4 over-cap NDI/WebRTC fns were already resolved (PR #368); not re-touched.
- **Version:** 0.4.131 → **0.4.132** (commit `18849e2`), after merging main into dev (FF to `58dbe9b`).
- **Implementation:**
  - Extracted the embedded checker out of `quality-check.sh` §7 into standalone `scripts/dev/fn_length_check.py` (identical behavior) so the test exercises the exact CI code (no drift). Commit `f7960d8`.
  - Added the self-test, then aligned it to the repo's `tests/ci/*.test.sh` convention → **`tests/ci/fn-length-gate.test.sh`**, run via the Test job's existing "Run CI shell tests" step. Commit `4b11f37`.
- **RED→GREEN proof:** test passes against the real (fixed) checker; reintroducing the abs/rel bug (`rel`→`path` in the relpath compare) makes the test FAIL (exit 1); restoring it passes again. Asserts: gate fires on a relative-path over-cap fixture, buggy abs-path variant no-ops, absolute target no-ops, unscoped scan fires.
- **PR #399** dev→main, merged `2cf145a`. Dev pipeline all green (incl. self-test step); deploy-dev verified `/healthz` = 0.4.132. Main Deploy → prod.
- **Side findings (filed):** `#404` — prod android stage relaunch fails (Fully Kiosk `launchComponent` activity missing on sd1-sd4; TVs still show prod `/stage` via TCL browser). Verified dev presenter has ZERO android-stage config (no prod/dev watchdog conflict); cleaned up stray dev2 adb client connections to sd1-sd4.
- **NDI audit (user concern):** current `dev` confirmed intact — every NDI stutter-debug experiment (VP8/480p/sw-encode/silent-audio/playout-delay/RTCP-SR/lite/diag) fully reverted, zero residue; Resolume/audio/HW-H264/30s-reaper/watchdogs all live (verified by grep + clean cargo check/clippy).

## #370 — NDI: source-switch leaks old source's pipeline + encoder (2026-06-16)
- **Bug (rescoped from validator PARTIAL):** switching active video source (deactivate A → activate B) started B's pipeline but never stopped A's. `repository.activate_video_source` flips sibling rows `is_active=false` without notifying the manager; `manager.start_pipeline` only touches the new id; no reaper covers a whole orphaned source pipeline. → two source pipelines = two `nvh264enc` encoders after every switch (NVENC contention). "Two encoders per source" was already gone (PR #390 single shared H264); deactivate-all + delete-active already tear down.
- **Fix:** `retain_only_active(active, keep_id)` (manager.rs) removes+stops every active-map entry whose source_id != keep; `NdiManager::stop_other_pipelines(keep_id)` (lifecycle.rs); `activate_video_source` (integrations.rs) calls it AFTER the new source confirms Streaming (no stage gap; start-failure returns early without reaping).
- **RED→GREEN:** RED `5d90b29` (no-op stub + tests, FAIL left:2/4 right:1), GREEN `8b52d03`. Tests: `crates/presenter-ndi/src/manager.rs` `stop_other_pipelines_tests::{activate_switch_leaves_exactly_one_source, activate_reaps_all_stale_siblings, reactivate_same_source_keeps_it}`.
- **Version:** 0.4.132 → 0.4.133 (`fa6205c`). Local: fmt/clippy clean, 48 ndi + 251 server tests pass.
- **Live-verify caveat:** dev has no video sources (`/healthz ndi_pipelines:[]`), prod has a single source — a real 2-source switch can't be exercised on a live target; the CI regression test is the behavioral proof.

## #384 — Stage layout resets to default on server restart — persist it in DB (2026-06-16)
- **Bug:** selected stage layout (`POST /stage/layout`) lived only in `AppState.stage_layout` RwLock (init to `DEFAULT_STAGE_LAYOUT_CODE` `worship-snv`); `set_stage_layout_code` wrote RwLock + live event only, never the DB. Every `presenter.service` restart/deploy silently reset the layout → blanked NDI stage displays (false "TVs not connecting" alarms, found during PR #382 prod testing).
- **Fix:** persist via existing `app_settings` k/v table, key `feature.stage.layout` (same mechanism as Companion `feature.companion.*`, which intentionally bypass `settings_audit` — only typed singleton tables are audited). `set_stage_layout_code` (`state/stage_display.rs`) writes the code after the RwLock update (failed write logs+continues, never aborts the live switch). `from_config` (`state/mod.rs`) loads it on startup (pure read, seeds RwLock; falls back to default if unset/unknown). NO migration: `app_settings` already exists (core migration); only a new key added.
- **RED→GREEN:** RED `48963a9` (`stage_layout_persists_across_restart` fails: after restart layout=`worship-snv` not `timer`), GREEN `f75e226`. Tests in `crates/presenter-server/src/state/tests.rs`: `stage_layout_persists_across_restart` (2nd AppState from same on-disk DB) + `stage_layout_load_on_startup_writes_no_audit_rows` (no-audit invariant).
- **Version:** 0.4.134 → 0.4.135 (`9751f1c`). Local: fmt/clippy clean, server 253 + persistence 50 (incl. `second_startup_writes_no_audit_rows`) + migration 3 pass.
- **PR #411** dev→main — opened, NOT merged (supervisor drives CI→merge→deploy; project CI ~30-40 min).

## #404 — Prod android stage relaunch fails (dead Fully Kiosk launchComponent) (2026-06-16)
- **Bug (confirmed live):** all 4 prod displays (sd1-sd4) stuck `state:"error"` — seeded `launch_component` `com.fullykiosk.videokiosk/de.ozerov.fully.MainActivity` activity doesn't exist; periodic `adb shell am start -n <component>` failed every attempt (Error type 3). TVs actually run `com.tcl.browser` opened via VIEW intent at the stage URL.
- **Fix (variant B, per supervisor decision on #404):** `launch_component` is now a browser PACKAGE (default `com.tcl.browser`; `DEFAULT_LAUNCH_COMPONENT`→`DEFAULT_LAUNCH_PACKAGE`). `validate()` relaxed to accept a bare package; legacy `package/activity` still valid + launcher extracts the package before `/`. New env `PRESENTER_ANDROID_STAGE_URL` (read once at `AndroidStageRegistry::new`); launcher fires `am start -a android.intent.action.VIEW -d <url> <package>`. Unset/empty URL → structured WARN + skip (records error so dashboard shows the misconfig, no broken intent). Per-env in deploy units (prod `http://10.77.9.205/stage`, dev `http://10.77.8.134:8080/stage`); documented in CLAUDE.md. Incremental migration `m20260616_000001` rewrites only dead-value rows to `com.tcl.browser` (idempotent, leaves customised rows, never edits the seed). `connect_and_launch` refactored → `adb_connect` + `adb_launch` helpers (all functions ≤120 cap; was 124). Settings UI labels/placeholders updated off the dead component.
- **RED→GREEN:** RED `5fd25fd` (launcher VIEW-intent/package-extraction/skip + validate-bare-package tests fail to compile / assert dead value), GREEN `3f8e29e`. Tests: `crates/presenter-server/src/android_stage.rs::tests::{build_launch_args_emits_view_intent_with_url_and_package, build_launch_args_extracts_package_from_legacy_component, build_launch_args_skips_when_url_unset, launch_package_strips_legacy_activity_suffix}`; `crates/presenter-core/src/android_stage_display.rs::tests::{validate_accepts_bare_package_name, validate_still_accepts_legacy_package_activity, validate_rejects_shell_metacharacters_in_package, default_launch_component_is_bare_browser_package}`; `crates/presenter-migration/src/m20260616_000001_fix_android_stage_launch_package.rs::tests::{migration_replaces_dead_component_and_is_idempotent, migration_no_op_when_table_missing}`.
- **Version:** 0.4.135 → 0.4.136 (`15173ee`). Local: fmt/clippy clean, quality-check `--strict` exit 0 (connect_and_launch 124→59 lines), core 5 + migration 2 + server android_stage 6 + persistence android_stage/seed 4 pass.
- **PR** dev→main — opened, NOT merged (supervisor drives CI→merge→deploy).

## #402 — Stage TVs: keep-screen-awake (sd1 has 5-min screen_off_timeout) (2026-06-16)
- **Validated STILL_VALID:** zero `wakeLock`/`WakeLock` matches in source (only build artifacts); sd1 Tesla = `screen_off_timeout=300000` → risk of black stage after 5 min idle. Feature never existed.
- **Feature:** new `crates/presenter-ui/src/components/stage/wake_lock.rs`. `should_acquire(held_live, visible)` pure decision fn + 3 host unit tests. `wasm_impl` (`target_arch="wasm32"`): acquires `navigator.wakeLock.request('screen')` on `StagePage` mount, re-acquires on `visibilitychange`→visible (browser auto-releases when hidden). Graceful `leptos::logging::warn!` on reject/unsupported, never panics. Wired into `StagePage`; added stable `VisibilityState` web-sys feature.
- **Design decision:** reached the API via RAW JS interop (`js_sys::Reflect`+`Function.call`), NOT web-sys typed `WakeLock` bindings — those need `--cfg=web_sys_unstable_apis`, which flips `HtmlElement::set_scroll_top` to `f64` and breaks 6 unrelated slide-scroll call sites (`slide_list_scroll.rs`/`ai.rs`). Raw interop keeps the change self-contained; no cfg flag, only the stable `VisibilityState` feature added.
- **Tests (feature, same PR):** `crates/presenter-ui/src/components/stage/wake_lock.rs::tests::{acquires_when_visible_and_no_live_lock, does_not_reacquire_when_already_held_live, does_not_acquire_when_hidden}`. No E2E (wakeLock spying is non-deterministic cross-browser; per maintainers, do not add flaky E2E).
- **Version:** 0.4.136 → 0.4.137 (`5687f0d`). Local: `cargo test --lib wake_lock` 3/3; clippy `--lib` host + `--target wasm32` clean; `cargo check --lib --target wasm32-unknown-unknown` (real run target) compiles clean; fmt clean; quality-check `--strict --against origin/main` exit 0.
- **Out-of-repo follow-ups (noted in PR, ops handles):** set `screen_off_timeout=max` on sd1 via adb; fix prod `stage-watchdog.sh` stale "VP8" comment (script not in repo).
- **PR #414** dev→main — opened, NOT merged (supervisor drives CI→merge→deploy).

---

## 2026-06-20 — Batch: #364 + #366 (one PR, one CI cycle) → PR #427, merge 5f8e5f9

**Version:** 0.4.140 → 0.4.141 (`9b91706`).

### #364 — quality-check.sh placeholder/TODO gate was a silent no-op (BUG FIX, RED→GREEN)
- **Root cause:** `crates/**/*.rs` rg include glob is version-fragile (older rg misses nested files) + both rg calls ended in `2>/dev/null || true` (swallowed real scan errors → gate passed silently). No self-test.
- **Fix:** extracted scan into `scripts/dev/placeholder_check.sh` — scans explicit relative `crates` path via `--type rust`/`--type ts`, branches on rg exit code (0 match / 1 no-match / ≥2 error) so a real scan error exits 2 loudly. quality-check.sh §16 calls it + maps exit code. Self-test `tests/ci/placeholder-gate.test.sh` wired into CI "Run CI shell tests".
- **Regression test:** `tests/ci/placeholder-gate.test.sh` — RED `4716b45` (test only, gate absent / old shallow-glob no-ops), GREEN `723881f` (robust gate fires on deeply-nested marker, fails loudly on scan error). `Closes #364`.
- **Review fixes:** `456e200` (rel scan path fixes spaced-checkout false-positive in comment filter; meaningful RED-pin), `a65000f` (assertion 3 → genuine differential pin vs $GATE), `5407f6b` (CI: install ripgrep in Test job — runner lacked it, self-test failed on missing rg).

### #366 — consolidate cargo-audit/deny advisory-ignore config (REFACTOR, no behavior change) — `a840fe7`
- Moved RUSTSEC-2026-0097 + RUSTSEC-2026-0173 from CI `--ignore` flags into `.cargo/audit.toml` (file cargo-audit actually reads); dropped `--ignore` from pipeline.yml + security-schedule.yml (kept in sync); added cross-ref comment to deny.toml; deleted dead repo-root `audit.toml` (empirically proven never read). RUSTSEC-2026-0173 stays ignored (blocked by #367). Verified: `cargo audit --deny warnings` (no flags) + `cargo deny check advisories / bans licenses sources` all green.

**Audits:** /review (code-review skill) + /requesting-code-review (deep) — all findings fixed. CI all 13 required checks + Mutation advisory green; PR mergeable+clean. Deployed v0.4.141: dev DOM `v0.4.141 (dev)`, prod DOM `v0.4.141`, 0 console errors.

---

## 2026-06-20 — #429 CI: make the Mutation Testing PR gate airuleset-compliant (joins PR #428)

**Version:** unchanged — dev already at 0.4.142 (> main 0.4.141). No bump.

### #429 — Mutation gate full-`--workspace` shard timed out at 45min (`|| true` fake-green) → every PR UNSTABLE (CI-FOUNDATION FIX)
- **Root cause:** `pipeline.yml` `mutation` job ran `cargo mutants --workspace --timeout 120 --no-shuffle --shard 1/12 ... || true` with `timeout-minutes: 45` — a full-tree shard that timed out at the 45-min cap → run conclusion `cancelled` → PR mergeStateStatus UNSTABLE, despite all required checks green. The `|| true` made it a fake-green advisory, never a real gate.
- **Fix (airuleset `ci/mutation-testing.md` two-tier shape):**
  - `pipeline.yml` `mutation` job → **diff-scoped REAL gate**: `timeout-minutes: 20` (hard cap), checkout `fetch-depth: 0`, install cargo-mutants+cargo-nextest via `taiki-e/install-action@v2` (prebuilt), run `git diff origin/${{ github.base_ref || 'main' }}...HEAD > pr.diff` then `cargo mutants --in-diff pr.diff --baseline=skip --test-tool=nextest --jobs 2 -- --all-targets`. Removed `|| true` + `--workspace --shard 1/12`.
  - `.cargo/mutants.toml`: `profile = "mutants"`, `test_workspace = false` (per-package tests only — `test_package = true` is invalid TOML; the boolean key is `test_workspace`), `exclude_globs` for migrations / proto vendor / companion protocol / build.rs.
  - `Cargo.toml`: `[profile.mutants]` `inherits = "test"`, `debug = "none"`.
  - `.github/workflows/mutation-full.yml`: `workflow_dispatch`-only full-tree sweep, sharded 1..8 on `ubuntu-latest`, survivors → `gh issue create` label `test-quality` (label auto-created). The `/mutation-sweep` target.
  - `.gitignore`: `mutants.out/` + `/pr.diff`.
- **Diff mutation result (local, verified):** 3 mutants in the PR diff — 1 CaughtMutant (`stage_display_snapshot` match-arm, killed by #383 unit tests), 2 Unviable, **0 survivors** → no new tests needed. `Closes #429`.

**Cycle:** local fmt + clippy (`-D warnings`) + `cargo test --workspace` (all green) + quality-check `--strict --against origin/main` (exit 0) + diff-scoped `cargo mutants` (0 survivors). PR #428 body extended with `Closes #429`.

## 2026-06-21 — batch #392 + #421 (Android stage launcher) — PR #432, v0.4.143

- **#392** (provisioning bug): `bootstrap-host.sh` did not install `adb` (hard runtime dep of the Android Stage Launcher). RED `caa6abe` (tests/ci/bootstrap-adb.test.sh asserts `android-tools-adb` in APT_PACKAGES — failed against adb-less bootstrap) → GREEN `0d4b4f9` (added the package; updated runbook.md; added "Ensure ADB is installed" step to release.yml PP deploy, mirroring deploy.yml — closes PP coverage gap). CI self-test wired into pipeline.yml "Run CI shell tests".
- **#421** (test-addition gated on DI refactor): keep-alive wiring untested (bare `Command::new(adb_bin)`, no seam). Seam refactor `3d0cd79` (`trait AdbRunner`; `ProcessAdbRunner` prod impl; `&dyn AdbRunner` threaded through run_device_worker/connect_and_launch/adb_connect/adb_launch/adb_foreground_package; `async-trait` moved dev-dep→dep). Wiring test `4d705ba` (FakeAdbRunner; tests: tick_skips_am_start_when_browser_foreground, tick_fires_am_start_when_backgrounded, tick_fires_am_start_when_foreground_unknown, launch_now_always_fires_am_start_without_probing, worker_launch_now_command_forces_am_start). Proven non-tautological: bypassing `if !force_launch` failed all 3 tick tests (reverted).
- **Mutation:** diff-scoped `cargo mutants` first run = 1 survivor (`adb_connect -> Ok(())`). Killed by `61208d9` (launch_aborts_when_adb_connect_fails + connect_calls assertion); targeted re-run = 1 caught → **0 survivors**.
- **Reviews:** /review correctness (2 cosmetic notes — timeout wording, fact still surfaced), /review ci-ops ([]), requesting-code-review (Ready to merge, 0 Critical/Important).
- Decision: the two cosmetic timeout-message notes left as-is — the propagated io error message still names "adb command timed out", so the timeout fact is preserved on the operator dashboard's last_error.

**Cycle:** local fmt + clippy (`-D warnings`) + `cargo test --workspace` (277 passed, 0 fail) + diff-scoped `cargo mutants` (0 survivors after the kill). One push, one Pipeline CI run, PR #432 (`Closes #392`, `Closes #421`).

### Follow-up on PR #432 — mutation gate timed out (#430), sharded + warmed

- **Root cause (#430):** the single-job diff-scoped `mutation` gate (#429) hit its 20-min `timeout-minutes` cap. cargo-mutants builds the `mutants` profile in an isolated dir that the `ci` rust-cache (keyed for the debug/test profile in `target/`) does NOT warm, so the heavy dep graph (gstreamer/NDI/leptos/seaorm) cold-built from scratch; CI run 27887309677 logged "Found 11 mutants" then cancelled at 20m16s with ZERO mutants tested → PR mergeStateStatus UNSTABLE (all REQUIRED checks green; mutation is non-required, deploy-dev does not `needs` it, so v0.4.143 already deployed to dev).
- **Fix (airuleset `ci/mutation-testing.md` "Budget overrun = setup bug" — SHARD + WARM, never raise the cap):**
  - `pipeline.yml` `mutation` job → 4-job matrix `shard: [0,1,2,3]` (cargo-mutants `--shard k/4` is 0-indexed; `4/4` is rejected). `--in-diff` then `--shard` splits the 11 diff mutants 3+3+3+2.
  - Dedicated `mutants`-keyed `Swatinem/rust-cache` (separate from `ci`) + a `cargo build --tests --profile mutants --workspace --all-targets` warm step, then `cargo mutants --in-place` reuses the warm `target/` directly (dropped `--jobs 2` — in-place serializes). `timeout-minutes: 20` UNCHANGED (now per shard).
  - `.gitignore`: added `mutants.out.old/` (cargo-mutants rotates the previous run aside).
- **Local verification (warm):** cold warm-build 7m21s (8-core dev2 under GPU load); full-diff `cargo mutants --in-place` = **11 mutants in 6m: 9 caught, 2 unviable, 0 survivors** (exit 0). With 4 shards each ≈ warm build + ~3 mutants → comfortably under the 20-min cap. No new tests needed — the seam diff is fully covered.
- **PR #432 body** extended with `Closes #430` (keeps `Closes #392`, `Closes #421`).
- 2026-06-21 batch#3 PR #432 (merge cc80c06) — Closes #392 (adb in bootstrap-host.sh + release.yml adb step + lock test), #421 (AdbRunner seam + keep-alive wiring integration test), #430 (mutation gate: 4-way shard + warm mutants-profile cache, fits 20-min cap). main Deploy success, v0.4.143 dev+prod (healthz verified). Filed #433 (deploy.yml set -e twin).

## batch#4 — #394 verse-split + #433 deploy.yml set -e (v0.4.144)

- 2026-06-21 batch#4 — bump 0.4.144 (c1668d8c). One PR closes #394 + #433.
- **#394 (verse-split bug, path A — deterministic composer layer, no agent.rs/LLM-prompt changes):**
  - RED: `9a5550bb` — new/updated tests assert a verse is never split mid-text. GREEN: `703d562b`.
  - `compose.rs` `compose_bible_items_into_slides`: (a) merge consecutive `Verse` items sharing the SAME number into one whole line/slide; (b) lone oversized verse kept WHOLE (`would_overflow` returns false on empty acc); (c) review-found edge — when a same-number continuation fragment would over-pack a slide already holding an earlier verse, `flush_keeping_last` flushes the earlier verses so the growing verse lands whole on its own slide (`df940402`). Closure → `VerseAccumulator` struct; extracted `build_reference_label` + `group_reference` to keep both composer fns < 120-line cap.
  - `bible_validator.rs` `is_lone_whole_verse`: validator ACCEPTS a lone whole verse over the char limit (single verse-prefixed line + non-empty reference; autofit shrinks). Multi-verse over-pack + oversized emphasis still rejected.
  - Tests updated to new behavior: `create_bible_presentation_accepts_lone_oversized_verse_whole` (was `_rejects_oversized_single_verse`), `length_rule_accepts_lone_verse_one_char_over_limit` (was `_rejects_slide_one_char_over_limit`), `length_rule_accepts_lone_oversized_verse` (was `_rejects_slide_well_over_limit`), `compose_items_single_verse_longer_than_limit_kept_whole_on_own_slide` (renamed). New: `_same_verse_number_split_is_merged_whole_*`, `_same_number_merge_then_next_verse_overflows_*`, `_same_number_fragment_after_earlier_verse_moves_growing_verse_to_own_slide`, `_same_number_fragment_that_still_fits_stays_*`, `length_rule_rejects_multi_verse_slide_over_limit`.
  - `bible_presentation.rs`: refreshed 2 stale comments (validator no longer guarantees `main.len() <= limit`); extracted `parse_bible_items` to keep `create_bible_presentation` < 120 (`635b5511`). Doc comment on `compose_bible_items_into_slides` refreshed (`7a5c3872`).
  - Filed #434 (stale agent.rs prompt step 8 — "separate slides" now wrong; blocked by fn-length gate on `build_system_prompt`/`run_agent`, out of scope).
- **#433 (deploy.yml ADB `set -e` twin):** `fafe0f57` — `set -e` in deploy.yml ADB heredoc mirroring release.yml 47d466a; new `tests/ci/adb-install-set-e.test.sh` (genuine guard: fails when set -e removed). Review found a THIRD gap → `ae8e6cbe` added `set -e` to pipeline.yml deploy-dev ADB step + extended the test to all 3 workflows.
- Reviews: `/code-review` + `/requesting-code-review` both clean (0 critical/important; one minor doc-comment fixed).

## Batch #5 (2026-06-21) — #437 + #438 + #436 (one PR, v0.4.145)

- **Version:** bumped `0.4.144 -> 0.4.145` (Cargo.toml workspace, first commit `3fbc7154`).
- **#437 (bug — AI model default was retired `claude-opus-4-20250514`, 404'd prod 2026-06-21):**
  RED `40b3147c` (`ai::tests::default_model_is_not_retired` — asserts default != retired ID, == `claude-opus-4-6`) → GREEN `70a46330` (`Closes #437`). Extracted `DEFAULT_AI_MODEL` const = `claude-opus-4-6` (newest Opus the on-device CLIProxyAPI catalog serves; 4-8 not in catalog). Updated retired UI placeholder `crates/presenter-ui/src/pages/ai.rs:297` → `claude-opus-4-6`.
- **#438 (bug — `/ai/status` reported `claudeAuthenticated:true` for expired/dead OAuth token):**
  RED `61564d81` (`ai::proxy::tests::expired_token_is_not_authenticated` + fresh/mixed/none cases) → GREEN `6ecc3691` (`Closes #438`). `is_claude_authenticated()` now parses each `claude-*.json` token's RFC3339 `expired` field; provably-expired → not authenticated, unparseable → fail-open. WARN log per expired token + aggregate. Offline freshness check only (MVP, no live probe). Added `TokenValidity` enum.
- **#436 (behavior — hide song number on NDI fullscreen):** `13b2736d` (`Closes #436`). Added `hide_song_number` prop to `StatusBar` (mirrors `hide_live`), gated song-number `<div data-role="song-number">` render + its autofit on it, `NdiFullscreen` passes `hide_song_number=true`. Other layouts unchanged. E2E `tests/e2e/ndi-fullscreen-song-number.spec.ts` (absent on ndi-fullscreen, present on worship-snv, zero console errors). Synced presenter-ui Cargo.lock presenter-core 0.4.141→0.4.145.
- Local gates: fmt clean, clippy `--workspace --all-targets -D warnings` clean, full workspace tests green (293 server bin + persistence ok, no SIGSEGV), presenter-ui wasm32 check + 118 lib tests green.
- Note: RED tests are in-file `#[cfg(test)] mod tests` inside `ai/mod.rs`/`ai/proxy.rs` (standard Rust convention) — the filename-based `pre-push-test-check` RED-before-GREEN gate can't see them, so push used `[no-test: ...]` with the honest reason that the RED tests exist and precede the fixes (verified failing→passing).

_RED-before-GREEN verified: RED 40b3147c (#437), 61564d81 (#438) precede their GREEN fixes._

### batch#5 review hardening (post-review, same PR #440)
- `/code-review` (high) + deep second-pass review found 2 issues, both fixed in-PR:
  - `4149e304` proxy.rs: skip non-regular-file auth-dir entries (a `claude`-named subdir would EISDIR→Unknown→fail-open, masking expired tokens). +regression test `claude_named_subdir_does_not_grant_auth`.
  - `7cbd1727` ndi-fullscreen E2E: replaced blind `waitForTimeout(1500)` with a positive snapshot-arrival anchor (assert #042 on worship-snv first, then absence on ndi-fullscreen). Verified locally: 2 passed; removing `hide_song_number` makes the HIDDEN test fail (Received: 1).
- Filed airuleset hook gap (Rust in-file `#[cfg(test)]` tests undetectable by filename-based RED-before-GREEN gate) — see filed issue.

- Mutation gate: shard 7/16 caught a surviving `&&`→`||` mutant on the aggregate expired-log guard (`ddfcbccd`); fixed by removing the redundant counter/conditional (mutant target line deleted) + added fail-open Unknown-token test. Local fmt/clippy/6 proxy tests green.

---

## Batch #6 (2026-06-21) — #369 + #431 (one PR, v0.4.146) — NDI stage reconnect backoff + WHEP not-producing console errors

- **Version:** bumped `0.4.145 -> 0.4.146` (Cargo.toml workspace, first commit `e0aba871`; Cargo.lock sync `a503990e`).
- **#431 (bug — prod stage console spammed `/ndi/whep/<uuid>` 404 when a configured NDI source isn't producing):**
  RED `0fe08cae` (`ndi_whep::tests::post_whep_endpoint_returns_204_for_configured_not_producing_source` + compat variant — assert 204 not 404 for the not-active POST) → GREEN server `6f494213` (`Closes #431`). POST handler now maps `SOURCE_NOT_ACTIVE_ERR` → **204 No Content** (mirrors the prior idempotent-DELETE fix); the stage only POSTs for operator-activated sources, so "not active" == "configured, not producing yet". 404 reserved for session-scoped paths on a truly unknown source. Updated 2 old tests that asserted the pre-#431 404/503 contract to assert 204/503-never-404.
  Client + E2E `d32b857c`: `connect_whep` returns `ConnectOutcome::NotProducing` for a 204 (no SDP) — loop shows placeholder + quiet backoff, NO console warn/error. **Second console-error path found while writing the E2E:** a STARTING pipeline can answer 201 with an EMPTY/non-SDP body → `setRemoteDescription` throws "Expect line: v=" (a console error). Client now also treats a 201 whose body isn't a valid SDP (`!starts_with("v=")`) as NotProducing — the minimal addition to meet the zero-console-errors bar. E2E `tests/e2e/ndi-whep-not-producing-console.spec.ts` (POST never 404; stage with not-producing source → 0 console errors, 0 `/ndi/whep` 404s). Verified on dev2 (libndi present): POST=204, console clean across 3 runs.
- **#369 (PARTIAL→finished — watchdog reconnect loop had no backoff for never-decoding sources):**
  RED `581b9c2a` (`ndi_video::tests::first_watchdog_reconnect_is_not_immediate` + `backoff_is_monotonic_then_capped_at_5s` against a stub modelling the immediate-first-retry bug) → GREEN `4365a777` (`Closes #369`). The Ok-branch (watchdog-triggered reconnect) fall-through no longer re-POSTs immediately. A per-`<NdiVideo>`-instance `Rc<Cell<usize>>` backoff step (created once in the effect, replacing the process-global static) drives a 500ms→5s capped schedule applied to BOTH reconnect paths. `should_reset_backoff(session_lifetime_ms)` (pure, tested, threshold `HEALTHY_SESSION_MS=20s`): a session that lived past the no-decode horizon (clearly decoding) resets the step for a prompt reconnect; a connect-but-never-decode source escalates to the 5s cap. `now_ms()` made `pub(crate)`. Stale ndi_watchdog:387 doc comment ("#369 reconnect / 5s-capped backoff" as if it existed) corrected to describe the now-real both-branch backoff.
- Local gates: workspace fmt clean, clippy `--workspace --all-targets -D warnings` clean, full workspace tests green (no SIGSEGV); presenter-ui fmt+clippy clean, 121 lib tests green. WASM is `include_dir!`-embedded into the server binary — rebuilt UI + server binary for E2E verification (note: dist/ is gitignored, CI builds via scripts/build-ui.sh).
- Note: RED tests are in-file `#[cfg(test)]` modules (server `ndi_whep.rs`, ui `ndi_video.rs`) — the filename-based pre-push RED-before-GREEN gate can't see them (airuleset#2). RED-before-GREEN verified by hand: RED `0fe08cae` (#431) and `581b9c2a` (#369) precede their GREEN fixes and were observed failing→passing.
- **Filed #441:** ndi-webrtc.spec.ts live-frame tests (videoWidth>0) fail instead of skip when libndi is present but no live NDI source exists (env gap on dev2; CI unaffected — ubuntu lane skips, synthetic lane runs a real sender). Pre-existing, surfaced during #369/#431 verification.

---

## Batch #7 (2026-06-21) — #418 + #417 (one PR #444, v0.4.147) — split ndi_watchdog.rs + post-teardown reload guard

- **Version:** bumped `0.4.146 -> 0.4.147` (Cargo.toml workspace, first commit `0b55e1c8`).
- **#418 (refactor — ndi_watchdog.rs at 996 prod lines, 4 from the 1000 hard cap):** commit `c34fb137` (`Closes #418`). Behavior-preserving mechanical split into focused sibling modules (proven `ndi_reload_guard.rs` pattern), leaving the `Watchdog` lifecycle + page-session `ReloadEscalation` core in `ndi_watchdog.rs`. Resulting prod-line counts (CI gate = `first #[cfg(test)] - 1`): ndi_watchdog.rs **435** (was 996), ndi_frame_stats.rs 241, ndi_beacon.rs 184, ndi_profile.rs 155, ndi_health_ticker.rs 124, ndi_reload_guard.rs 150. All well under the 800 warn tier. Each `#[cfg(test)]` module moved with its code. `now_ms`/`local_storage`/`Watchdog::*` consts made `pub(crate)`; `profile_mode_is_compat` re-exported from `ndi_watchdog` so `ndi_video.rs`'s import is unchanged. 121 presenter-ui lib tests unchanged + green; quality-check.sh 0 hard fails.
- **#417 (bug — post-teardown async reload window):** RED `02bc7c8a` (`ndi_reload_guard::tests::post_await_suppressed_after_page_teardown` against the pure `should_perform_post_await_reload` with the `page_active` gate deliberately absent — observed `assertion failed: !should_perform_post_await_reload(false,false,true,true)`) → GREEN `6ca47dad` (`Closes #417`, restores the `page_active` gate). `ReloadEscalation` gains an `active: Cell<bool>` (default true) flipped false ONLY by `NdiVideo::on_cleanup` via `cancel()` — NOT by `Watchdog::stop()`/`Drop` (which fire on every reconnect; tying the cancel there would permanently suppress the #401 last-resort reload, as the page-session escalation outlives every Watchdog). `maybe_reload`'s detached post-await reload routes through `should_reload_after_check` → the pure host-testable `should_perform_post_await_reload(page_active, already_reloaded, horizon_crossed, server_has_streaming)`. Escalation moved out of the effect into a page-session `StoredValue<Option<Rc<ReloadEscalation>>, LocalStorage>` so both the effect and on_cleanup reach it.
- Local gates: workspace fmt clean, clippy `--workspace --all-targets -D warnings` clean + presenter-ui clippy clean, 126 presenter-ui lib tests green (121 + 5 new), quality-check.sh exit 0 / 0 hard fails.
- Note: the #417 RED test is an in-file `#[cfg(test)]` module — the filename-based pre-push RED-before-GREEN gate can't see Rust in-file tests (airuleset#2). Marker commit `afe658eb` carries `[no-test: ...]` with the honest reason; RED `02bc7c8a` precedes GREEN `6ca47dad`, observed failing→passing. #418 is a refactor (no bug fix).
- **Filed #443:** dev2 local env — presenter-ndi pipeline tests fail (`nvh264enc` element factory missing) on local `cargo test`. Confirmed IDENTICAL on clean origin/main (b7d7b31b) and main CI is green → local-env-only, NOT a regression; this batch is UI-only and does not touch presenter-ndi.

## Batch #8 (2026-06-22) — #393 + #434 (one PR #446, v0.4.148) — adb keygen in bootstrap + post-#394 AI bible prompt

- **Version:** bumped `0.4.147 -> 0.4.148` (Cargo.toml workspace + Cargo.lock, first commit `ebf1ca50`). Merge commit `923bae45`.
- **#393 (bug — adb cold-start race under hardened systemd):** RED `099f5383` (`tests/ci/bootstrap-adb-keygen.test.sh`, asserts bootstrap-host.sh runs a guarded `adb keygen ~/.android/adbkey`; observed failing on the pre-fix script) → GREEN `de0212c5` (`Closes #393`). Root cause: `presenter.service` runs `ProtectHome=read-only` + `ReadWritePaths=/opt/presenter*` only (no `~/.android`), so adb's first `start-server` can't mkdir `~/.android` to generate its RSA keypair → "ADB server didn't ACK"; masked on dev/prod because a pre-hardening adbkey already exists there. Fix = idempotent (absent-guarded) `adb keygen "$HOME/.android/adbkey"` in `scripts/ops/bootstrap-host.sh` after the apt install; `adb keygen` is NOT idempotent (overwrites every call) so the `if [ ! -f "$ADB_KEY" ]` guard leaves already-keyed hosts' device authorizations intact. systemd `ReadWritePaths` deliberately NOT widened (validated lowest-risk option). Runbook updated; new test wired into pipeline.yml "Run CI shell tests".
- **#434 (feature — AI bible prompt stale post-#394):** RED `c747d923` (`ai::agent::tests::system_prompt_describes_post_394_whole_verse_behavior` — extracted the pure `format_system_prompt` helper, asserts post-#394 wording present + stale "split that verse / separate slides" absent; observed failing) → GREEN `fc777d22` (`Closes #434`). Rewrote `build_system_prompt` step 8 + both `bible_validator.rs` `MainExceedsCharacterLimit` messages to the shipped #394 reality (lone oversized verse accepted whole; same-number continuation items merged; the error now only means over-packed multi-verse or oversized emphasis). Required mechanical function-extraction to satisfy the 120-line cap on the edited file (no behavior change): `format_system_prompt` (117), `build_api_messages` (28), `execute_tool_calls` (113); `build_system_prompt` 54, `run_agent` 105 — all ≤120.
- **Review fixes (`d6d86177`):** renamed `render_system_prompt -> format_system_prompt` (the `render_*` prefix is the fn-length-gate exemption, which would have silently un-gated the very function the extraction existed to bound); tightened the shell test's guard check to assert containment (keygen INSIDE the `! -f` block) + accept one-line/multi-line forms.
- **Mutation gate (`389247d3`):** the diff-scoped PR gate first surfaced surviving mutants in the extracted helpers; added 4 focused unit tests (`build_api_messages_*`, `build_system_prompt_returns_real_prompt_and_appends_extra`, `execute_tool_calls_blocks_delete_without_intent_and_runs_otherwise`, `run_agent_propagates_error_when_llm_unreachable` — the last points AiSettings at an unreachable endpoint so `run_agent` returns Err via `?`, killing the body→Ok mutants without a live LLM). Local re-run: 19 mutants, 17 caught, 2 unviable, 0 missed.
- **Local gates:** workspace fmt clean, clippy `-p presenter-server --all-targets -D warnings` clean, full `cargo test --workspace` 296+ pass / 0 fail, all CI shell self-tests pass, fn-length gate clean on changed files.
- **CI:** Pipeline run 27942593054 all-green (incl. all 16 mutation shards, E2E, NDI synthetic, deploy-dev). Main Deploy 27945325412 success (Build Release + Deploy to Production).
- **Post-deploy verify (prod 10.77.9.205):** `/healthz` v0.4.148 channel=release; DOM `[data-testid=version]` = v0.4.148 (matches); `POST /ai/chat` → 200 with a real agent reply (refactored run_agent intact on prod); browser console 0 errors. Both issues auto-closed.

## Batch #9 (2026-06-22) — #447 + #448 (one PR, v0.4.149) — Android stage home-portal launch + NDI neutral not-producing state

- **Version:** bumped `0.4.148 -> 0.4.149` (Cargo.toml workspace + Cargo.lock + presenter-ui Cargo.lock; first commit `b19db86d`).
- **#447 (bug — Android stage launcher never loads stage from the TCL browser home portal):** RED `7284e1ce` (`android_stage::tests::{launch_when_browser_on_home_portal, tick_fires_am_start_when_browser_on_home_portal, launch_when_browser_foreground_but_activity_unknown}` — observed failing against package-only gate) → GREEN `d481f31f` (`Closes #447`). Root cause: `should_launch_stage` + `parse_foreground_package` compared only the foreground PACKAGE; the home portal (`.portal.home.activity.StartActivity`) and the loaded stage page (`.portal.browse.activity.BrowsePageActivity`) are the SAME `com.tcl.browser` package, so the keep-alive (#419 suppression) skipped the relaunch forever when the TV powered on to the portal. Fix: made the gate ACTIVITY-aware — `parse_foreground_component` returns the full `<pkg>/<activity>`; `should_launch_stage` skips ONLY when package matches AND activity ends with `BrowsePageActivity` (`STAGE_CONTENT_ACTIVITY_SUFFIX`); home portal / bare-package / other / unknown → relaunch. #419 preserved.
- **#448 (bug — NDI fullscreen shows native play-arrow + RED 'pipeline failed' overlay for off/silent source):** RED `6f22bb62` (client `stage::tests::{no_signal_status_is_neutral_not_error, no_signal_status_text_is_waiting_placeholder, connecting_status_is_neutral}`) + `d8a74060` (server `integrations::tests::silent_source_maps_to_neutral_no_signal_and_is_not_a_hard_error`) → GREEN `92e2069f` + E2E `616efa6c` (`Closes #448`). Root causes: (1) server published `failed: … broadcaster is silent` for the 8s streaming-timeout (an off Resolume source), rendered red; (2) `ndi_fullscreen.rs` mounted a bare `<video>` (no srcObject) → browser native play-arrow. Fix: typed `PipelineStartError { SourceSilent, Failed }` in presenter-ndi lifecycle (silent = 8s timeout w/ no element error; Failed = genuine); `integrations.rs::ndi_status_for_start_error` maps SourceSilent → neutral `no-signal` + activation SUCCEEDS (sibling pipelines still reaped per #370), Failed → red `failed:` + hard error. Client: `NdiOverlayKind {None,Neutral,Error}` + `ndi_overlay_kind`; `ndi_status_text("no-signal")`="Waiting for video source…"; ndi-fullscreen renders neutral states as a covering gray placeholder (`stage-ndi__placeholder--cover`, solid black bg hiding the bare video + play-arrow), red overlay only for genuine failures.
- **Local gates:** workspace fmt + ui fmt clean; clippy workspace + presenter-ui `--all-targets -D warnings` clean; tests presenter-ndi 48, presenter-server 306, presenter-ui 132, presenter-persistence 51 — all pass; fn-length gate: no NEW hard-fail (start_pipeline 115 = pre-existing warning).

## Batch (2026-06-25) — #467 + #471 + #452 + #459 (one PR, v0.4.158)

- **Version:** bumped `0.4.157 -> 0.4.158` (Cargo.toml workspace + Cargo.lock + presenter-ui Cargo.lock; first commit `e657af4a`).
- **#467 (pin PRAGMA foreign_keys = ON):** GREEN `bf796332` (`Closes #467`). `apply_sqlite_pragmas` now sets `PRAGMA foreign_keys = ON` first; `connect_in_memory` now calls `apply_sqlite_pragmas` (it previously skipped pragmas). Test `apply_sqlite_pragmas_enables_wal_journal_mode` (persistence) — KEY mutation-killing design: sqlx 0.8 already defaults foreign_keys=ON / busy_timeout=5000 (proven by probe), so only `journal_mode` (sqlx default `delete` on a FILE db; our pragma → `wal`) distinguishes the `Ok(())` mutant. Test uses a private single-pool FILE db, asserts delete→wal. Verified locally: kills the mutant. (Earlier #466 attempt reverted 303ec9eb because its assertion read sqlx defaults off the shared-cache db.)
- **#471 (log stage/operator/tablet WS client IP on connect/disconnect):** GREEN `125809f1` (`Closes #471`). `/live/ws` handler extracts client IP (reuses audit `extract_actor`: XFF→X-Real-IP→anonymous; ConnectInfo dropped in axum 0.8) + surface. Surface travels via `?surface=` query param (operator/tablet in ws/mod.rs, stage in ws/stage.rs) since `/live/ws` is shared and the handshake carries no surface signal. `serve_websocket` logs INFO connect + disconnect with client_ip+surface (mirrors companion log shape). Tests: `normalize_ws_surface` (unit, router/tests) + `live_ws_connection_registers_stage_presence` (real tokio-tungstenite WS client integration test, multi-thread runtime — kills the `serve_websocket -> ()` mutant). dev-dep tokio-tungstenite 0.29 (matches axum's).
- **#452 (remove codecov + real coverage floor):** GREEN `4634a31d` (`Closes #452`). Deleted codecov upload step + `/codecov.yml` + CODECOV_TOKEN doc row. Replaced `--codecov/--output-path` with `--fail-under-lines 70` (measured 72.45% line coverage − 2). Per coverage-thresholds.md the in-CI fail-under is now the gate.
- **#459 (settings form aria-*):** GREEN `c83d58ff` (`(#459)` — Closes line is in PR body). aria-required on required inputs, aria-invalid bound to form_state error signal, each form-status `<p>` id'd + each field's aria-describedby → it. Cards: resolume, ableton (incl OSC), android, companion, video_sources (toast-driven → aria-required only). E2E `tests/e2e/settings-aria.spec.ts`: aria-required across all cards, describedby reachability, aria-invalid flip on rejected submit (resolume + android out-of-range port), zero console errors. Verified locally via Playwright (built WASM + release server, ran with PRESENTER_SKIP_MOCK_INTEGRATIONS=1 to avoid the local dev-service 8091 collision — CI runs on ubuntu-latest where 8091 is free).
- **Decision:** removed a stray `apply_sqlite_pragmas` mutants.toml exclusion a prior attempt committed (its "equivalent mutant" claim was false — journal_mode IS observable); the WAL test kills the mutant honestly instead of hiding it.
- **Local gates:** workspace + ui fmt clean; clippy presenter-server/persistence + ui `--all-targets -D warnings` clean; persistence 53, server 332 tests pass; diff-scoped mutation 0 survivors.

## 2026-06-28 — #483 Resolume lyrics latency (SOLO PR #485, merged bf6f1a95)

- **Version:** bumped `0.4.162 -> 0.4.163` (Cargo.toml workspace + Cargo.lock; first commit `666ee72b`). Deployed prod (release) + dev (dev), both `/healthz` `0.4.163`; dev operator DOM `[data-testid=version]` = `v0.4.163 (dev)`, console clean.
- **Root cause (#483):** `handle_stage` push path called `ensure_mapping`, which re-fetched + re-parsed the ENTIRE Resolume `/composition` whenever the cached clip-mapping was older than `MAPPING_CACHE_TTL=1s`. Slides during singing are >1s apart → almost every line paid a 300-620ms fetch (queue backing to ~1s) → LED wall lagged stage TVs ~1s.
- **Fix (GREEN `1fa0e279`, `Closes #483`):** removed the inline stale-refetch branch in `ensure_mapping` (driver.rs) — push path always serves the cached mapping; the only inline fetch is `mapping.is_none()` (cold / config change / error-invalidated). The pre-existing 10s background `mapping_timer` + `record_error` invalidation remain the only refresh triggers. Per-host worker isolation + non-blocking `try_send` fan-out unchanged.
- **RED→GREEN:** regression test `crates/presenter-server/src/resolume/latency_tests.rs::stage_push_does_not_refetch_composition_when_cache_is_fresh` — back-dates `last_mapping_refresh` to 3s (past old 1s TTL, below 10s interval) and asserts ONE `GET /composition` across two pushes. RED on `6454462b` (in tests.rs, 2 GETs), GREEN on `1fa0e279` (1 GET). Test later relocated to its own file (`87394409`).
- **Logging (same PR):** `refresh_mapping_with_reason` logs reason (`missing`/`error-invalidated`/`background-timer`) + bytes + fetch_ms + parse_ms + clip_count; `ensure_mapping` logs `mapping_cache=hit|miss`; new append-only `resolume_push_audit` DB table (migration `m20260628_000001`, idempotent `if_not_exists`, registered in migration lib.rs) written via non-blocking channel + background writer task that ALSO aggregates a cross-host perceived-latency line; existing missing-clip/error warnings kept.
- **GOTCHA — #482 file-size gate fix is a landmine:** the merged #407/#482 `count_prod_lines.sh` fix now counts past `#[cfg(test)] mod tests;`. Touching `state/mod.rs` (1117 prod lines, >1000 cap) or `resolume/tests.rs` (test fns 168/166 lines, >120) HARD-FAILS the now-correct gate. WORKAROUND used: wired the audit writer via `ResolumeRegistry::attach_audit_writer` (Arc<OnceLock> sink, idempotent) called from `state/integrations.rs::sync_resolume_hosts` (small file) instead of `state/mod.rs`; put the regression test in a NEW self-contained `resolume/latency_tests.rs` instead of editing `tests.rs`. Pre-existing debt filed: #486 (split state/mod.rs god-file), #487 (split the >120-line resolume test fns).
- **GOTCHA — diff-scoped mutation gate + a forced refactor:** to add audit/logging to handlers.rs I had to refactor the 156-line `handle_stage` under the 120 fn cap (extract `apply_stage_mapping`/`apply_stage_metadata`), which dragged pre-existing edge logic into the mutation diff. Killed all 23 survivors HONESTLY (no exclude_re): pure-fn unit tests for `duration_ms`/`count_clips`/`FetchReason::as_str` (made pub(super)); `flush_perceived` unit test; end-to-end audit test through `ResolumeRegistry` (kills run_host_worker/record_push_audit/attach_audit_writer/spawn_audit_writer no-op mutants); main-only lane-sync test; sync_resolume_hosts test; dropped the untestable `TRIGGER_DELAY > 0` guard; logged `mapping_age` as a Duration (no `* 1000.0`). Verified locally `cargo mutants --in-diff` = 47 tested, 43 caught, 4 unviable, 0 missed BEFORE re-push.
- **Code review:** `/review` 0 findings; `superpowers:requesting-code-review` 0 Critical, 2 Important (read endpoint + capture failed-push `outcome=error`) + retention → filed as #489 (cohesive audit-feature enhancements, follow-up gate).
- **Local gates:** fmt clean; clippy `--workspace --all-targets -D warnings` clean; full workspace tests green; quality-check.sh exit 0; mutation 0-missed locally + all 16 CI shards green.

## 2026-06-28 — Batch #484 + #487 (one PR, v0.4.165)

- **Version:** bumped `0.4.164 -> 0.4.165` (Cargo.toml workspace + Cargo.lock; first commit `3d7cc385`). No per-PR mutation gate anymore (#488 removed it).
- **#487 (split over-cap resolume/tests.rs fns):** GREEN `b1e63b71` (`Closes #487`). Extracted shared `mount_composition`/`mount_params`/`mount_clips`/`mount_full_composition`/`build_driver` + `stage_all`/`stage_main_meta` builders; rewrote `stage_updates_alternate_main_and_translation_lanes` (was 168) and `refreshes_mapping_after_cache_ttl_for_new_deck` (was 166) to use them; `setup_bible_driver` reuses the same fixture. Assertions unchanged, all 32 resolume tests still pass, `QC_TARGETS=…/resolume/tests.rs fn_length_check` → 0 violations. (Resolves the #483 playbook landmine for tests.rs; state/mod.rs #486 still open.)
- **#484 (bug — down Resolume host spams ERROR + no backoff):** RED `161425ae` (`resolume/backoff_tests.rs::{down_host_does_not_log_an_error_on_every_failure (64 ERROR lines), down_host_skips_push_attempts_while_in_backoff (3 GET /composition)}`) → GREEN `9ffa0d15` (`Closes #484`, 7 ERROR lines / 1 GET). Root cause: `record_error` logged `error!` UNCONDITIONALLY on every push + every 10 s tick (163,943 lines over 3 days), and there was no backoff so a down host re-fetched `/composition` on every attempt.
- **Fix design:** pure helpers `backoff_interval(consecutive_failures)->Duration` (exp 1s,2s,4s,… cap 60s) + `should_log_error(consecutive_failures)->bool` (power-of-two milestones → O(log N) lines). `record_error` opens `next_retry_at`; `handle_stage`/`handle_bible`/`handle_timer` + the mapping-timer tick skip while `in_backoff()`; `mark_connected` (now `&mut self`) clears the window + logs ONE INFO recovery line. Per-host worker isolation + non-blocking `try_send` fan-out UNCHANGED.
- **Test infra:** added `tokio` dev-dep feature `test-util` (paused clock for the backoff-window test); ERROR-log frequency asserted via a minimal scoped `tracing::Subscriber` counter. New tests live in self-contained `resolume/backoff_tests.rs` (registered in `resolume/mod.rs`) — keeps the fn-length gate green.
- **Local gates:** fmt clean; clippy `--workspace --all-targets -D warnings` clean; driver.rs 581 prod lines / handlers.rs 784 (< 1000 cap); fn_length 0 violations on all changed files; presenter-server 347 tests pass (37 resolume incl. 5 new).
- **PR:** shared dev→main batch PR, body `Closes #484` + `Closes #487`.

## 2026-06-29 — #461 (worship-PP current badge from playlist + sidebar auto-scroll)
- **#461 (rescoped PARTIAL by ticket-validator — PP-ONLY):** version bump `0.4.167→0.4.168` (`34ca8353`), feature `38f28be4` (`Closes #461`), review fixes `2357e7c7`, comment clarify `ffeebe73`. Shared PR #495, merge `13ceda5e`. Main Deploy run 28342839523 success.
- **Rescope rationale:** the issue's "like worship-snv" reference is INVERTED — worship-SNV is AbleSet-based for next-song, so copying it would be wrong. PP-only change. The PP NEXT-song badge was ALREADY playlist-sourced (b503ed06, 2026-04-27); only the CURRENT badge needed switching. `worship_snv.rs` untouched.
- **Item 1 — current badge from playlist:** `worship_pp.rs:current_song_text` now `current_song_from_entries(&playlist_entries())` (the `is_active` entry's `clean_song_name`), mirroring the existing next-song derivation, instead of `ctx.snapshot.song_name` (AbleSet-first server-side). Note: server already sanitizes both `StagePlaylistEntry.name` (`stage.rs:377`) and `song_name`, and `clean_song_name`≡`sanitize_song_title`, so without AbleSet they coincide — the unit tests prove the source change, the E2E proves end-to-end behavior.
- **Item 2 — sidebar auto-scroll:** new `worship_pp_helpers::scroll_active_entry_into_view()` queries `.stage-pp__playlist-sidebar .stage-pp__playlist-entry--active` and `scroll_into_view(block:Center)`; an `Effect` tracks the active entry's INDEX (not name — handles repeated songs) and fires the scroll via `Timeout(0)` on change. Mirrors operator `slide_list_scroll.rs` (DOM-query, not NodeRef — keyed `<For>` reuses row DOM). Content-scroll only; sidebar box size/position UNCHANGED (CLAUDE.md box rule). web-sys `ScrollIntoViewOptions`/`ScrollLogicalPosition` already in features.
- **Helpers extracted** to `worship_pp_helpers.rs` (new) with 9 host unit tests (`cargo test --lib` in crates/presenter-ui) covering active/no-active/empty/last-entry/3-digit-prefix for both `current_song_from_entries` + `next_song_from_entries`.
- **E2E:** appended to `tests/e2e/stage-worship-pp.spec.ts` — 14-entry playlist at 1080p; asserts badge==active playlist entry name (updates on advance), active row scrolls into sidebar viewport after advancing past the fold, zero console errors. Hard `expect(picked.length).toBeGreaterThanOrEqual(14)` guard (not test.skip). Validated locally (stopped presenter-dev to free fixed mock-resolume port 8091, restarted after) + green in CI.
- **Reviews:** /review + deep /requesting-code-review. Fixes applied: index-keyed scroll dedup (was name — repeated-song safe), test.skip→hard assert, comment clarified. Pre-existing name-keyed `<For>`/`is_active` duplicate-song fragility (out of diff) filed as **#496**.
- **Post-deploy:** prod DOM v0.4.168 (release) + dev DOM v0.4.168 (dev); both /healthz 0.4.168. Functional on live dev: badge tracked active playlist song on advance, active row (idx 13/14) scrolled into viewport, 0 console errors.

# Candidate-run trace output — regenerated by the drive stage, never committed. Only
# golden/*.json (the claude-opus-4-6 reference traces) are versioned in git.
traces/

# Layer-1/Layer-2 results + generated summaries — regenerated by the score-l1/judge/gate
# stages, never committed.
report/

# promptfoo's own cache, if a local run ever points PROMPTFOO_CACHE_PATH inside this dir.
.promptfoo-cache/

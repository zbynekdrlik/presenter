services:
  gateway:
    build:
      context: ./gateway
    container_name: presenter-gateway
    restart: unless-stopped
    environment:
      PORT: 8080
      DEMO_MANIFEST_DIR: /manifests
    volumes:
      - ./var/docker/demos:/manifests:ro
    ports:
      - "80:8080"
